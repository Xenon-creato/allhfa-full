import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "https://jalisa-veritable-spirituously.ngrok-free.dev",
    "http://jalisa-veritable-spirituously.ngrok-free.dev",
  ],
};

export default nextConfig;
